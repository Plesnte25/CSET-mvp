services:
  app:
    build: .
    ports: ["8501:8501", "8000:8000"]
    volumes: ["./:/code"]
    command: >
      sh -c "streamlit run src/gui.py & uvicorn src.api:app --host 0.0.0.0 --port 8000"
  baserow:
    image: baserow/baserow:1.25.0
    env_file: .env
    ports: ["80:80"]
